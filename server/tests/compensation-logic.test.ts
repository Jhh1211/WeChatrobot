import { describe, expect, test } from "vitest";

describe("scheduler (self-hosted)", () => {
  test("compensation via WorkTool removed; use Android inbound-events instead", () => {
    expect(true).toBe(true);
  });
});
